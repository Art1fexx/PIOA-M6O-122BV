# Лабораторная работа №3 : In-Memory база данных автомобилей

## Германов Дмитрий/М6О-122БВ-25

Консольное приложение на Python для CRUD-операций над таблицей автомобилей с хранением данных в оперативной памяти. Реализовано с использованием объектно-ориентированного подхода и покрыто тестами.

## Структура проекта

PIOA-M6O-122BV/
│
├── src/
│ └── db/
│ ├── init.py
│ ├── main.py
│ ├── tui.py
│ └── backend/
│ ├── init.py
│ ├── memory.py
│ └── errors.py
│
├── tests/
│ ├── init.py
│ ├── test_memory.py
│ ├── test_tui.py
│ └── test_main.py
│
└── README.md



## Реализованная функциональность

### Добавление записи
Пользователь последовательно вводит:
- ID (целое число, уникальное)
- Бренд (обязательное поле)
- Модель (обязательное поле)
- Год производства (целое число, от 1900 до 2026)
- Страна (опционально)

**Проверки:**
- ID не должен совпадать с существующим
- Год производства должен быть в диапазоне 1900-2026
- Бренд и модель не могут быть пустыми
- При успехе запись добавляется в таблицу и выводится на экран

### Просмотр
- Вывод всех записей таблицы в табличном виде

### Поиск
- Фильтрация записей по любому полю (ID, бренд, модель, год, страна)

### Обновление
- Изменение одного или нескольких полей существующей записи по ID

### Удаление
- Удаление записи по ID

## Запуск программы

Из корневой директории проекта:

```bash
python -m src.db

#Запуск всех тестов
python -m pytest tests/

# Установка pytest-cov (если не установлен)
pip install pytest-cov

# Запуск с измерением покрытия
python -m pytest --cov=src --cov-report=term-missing tests/
